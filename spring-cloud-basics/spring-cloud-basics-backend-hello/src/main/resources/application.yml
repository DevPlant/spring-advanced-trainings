spring:
  profiles:
    active: dev
  application:
    name: spring-cloud-basics-backend-hello

logging:
  level:
    org.springframework.security: INFO

eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 7070
